# Secrets & per-user state — never commit (each user generates their own)
.secrets.env
deploy.conf
clash-configs/
profiles/

# Personal planning docs — not part of the shareable tool
VPN方案设计.md

# OS / editor cruft
.DS_Store
*.swp
*.swo
__pycache__/
*.pyc
.venv/
.idea/
.vscode/

# Local Graphify analysis cache (may contain absolute paths and derived metadata)
config/mcporter.json
graphify-out/
